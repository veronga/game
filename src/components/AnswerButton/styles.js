import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    width: 160,
    height: 50,
    borderRadius: 35,
    justifyContent: 'center',
  },
  titleStyle: {
    fontSize: 18,
    fontWeight: 'bold',
    alignItems: 'center',
    textAlign: 'center',
    color: '#FFFFFF',
  },
});
