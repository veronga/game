import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  textStyles: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#fff',
    alignSelf: 'center',
    marginTop: 40,
    marginBottom: 40,
  },
  customStyles: {
    marginTop: 50,
  },
});
